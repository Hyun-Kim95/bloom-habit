# 백로그 / TODO — 관리자 페이지 (PRD J 기준)

**PRD J. 관리자 페이지**

- **목적**: 운영자가 서비스 핵심 데이터를 조회/관리할 수 있도록 한다.
- **상세 요구사항**: 관리자 로그인, 회원 조회, 습관 템플릿, 공지/운영 콘텐츠, AI 문구·프롬프트, 통계 대시보드, 설정값 관리 검토.

---

## 현재 상태

| 구분 | 상태 | 비고 |
|------|------|------|
| 관리자 **API** (서버) | ✅ 완료 | 로그인, 회원, 통계, 템플릿, 공지, system-config |
| 관리자 **페이지(UI)** | ❌ 미구현 | API 호출용 화면 없음 |

---

## Todo 리스트 (관리자 페이지)

### 1. 기반

- [ ] **1.1** 관리자 페이지 진입 경로 결정 (웹 전용 URL vs Flutter 웹/앱 내 숨김 메뉴)
- [ ] **1.2** 관리자 프론트 프로젝트 생성 (웹: React/Vue/HTML 등, 또는 Flutter web 라우트)
- [ ] **1.3** 관리자 API 클라이언트·인증 연동 (로그인 후 토큰 저장, 요청 시 Bearer 첨부)

### 2. 관리자 로그인

- [ ] **2.1** 관리자 로그인 화면 UI (이메일, 비밀번호 입력)
- [ ] **2.2** `POST /admin/auth/login` 호출 및 `accessToken` 저장
- [ ] **2.3** 로그인 실패 시 에러 메시지 표시
- [ ] **2.4** 로그인 성공 시 대시보드(또는 첫 메뉴)로 이동, 미로그인 시 로그인 페이지로 리다이렉트

### 3. 회원 조회 (기본 사용자 상태 확인)

- [ ] **3.1** 회원 목록 화면 (테이블 또는 카드)
- [ ] **3.2** `GET /admin/users` 연동 — id, email, displayName 등 표시
- [ ] **3.3** (선택) 사용자 상태 표시/필터 — PRD “기본 사용자 상태” 반영 여부 결정 후 구현

### 4. 습관 템플릿 등록 / 수정 / 삭제

- [ ] **4.1** 습관 템플릿 목록 화면 (`GET /admin/habit-templates`)
- [ ] **4.2** 템플릿 등록 폼 + `POST /admin/habit-templates` 연동
- [ ] **4.3** 템플릿 수정 폼 + `PATCH /admin/habit-templates/:id` 연동
- [ ] **4.4** 템플릿 삭제 + `DELETE /admin/habit-templates/:id` 연동 (확인 다이얼로그 권장)

### 5. 공지 / 운영성 콘텐츠 관리

- [ ] **5.1** 공지 목록 화면 (`GET /admin/notices`)
- [ ] **5.2** 공지 등록 + `POST /admin/notices` (제목, 본문, publishedAt 등)
- [ ] **5.3** 공지 수정 + `PATCH /admin/notices/:id`
- [ ] **5.4** 공지 삭제 + `DELETE /admin/notices/:id`

### 6. AI 기본 문구 또는 프롬프트 관리

- [ ] **6.1** 시스템 설정(또는 AI 문구) 화면 — `GET /admin/system-config` 연동
- [ ] **6.2** AI fallback 문구 목록 조회·표시 (예: `ai_fallback_messages` JSON)
- [ ] **6.3** fallback 문구 수정 UI + `PATCH /admin/system-config` 연동
- [ ] **6.4** (선택) 프롬프트 관리 — 서버에 프롬프트 설정 API가 있으면 해당 화면 추가

### 7. 기본 통계 대시보드

- [ ] **7.1** 대시보드 레이아웃 (카드/섹션 구분)
- [ ] **7.2** `GET /admin/stats` 연동 — totalUsers, totalHabits, totalRecords 표시
- [ ] **7.3** (선택) 차트·추가 지표 — 필요 시 API 확장 후 UI 반영

### 8. 설정값 관리 가능 여부 검토

- [ ] **8.1** “설정값 관리” 범위 정의 (예: JWT 만료, 일일 AI 호출 상한, 기능 플래그 등)
- [ ] **8.2** 검토 결과: 관리자 페이지에서 노출할 설정 목록 결정
- [ ] **8.3** (결정 시) 설정 조회/수정 API·UI 구현

### 9. 공통·마무리

- [ ] **9.1** 관리자 전용 라우트 가드 (미로그인 시 로그인 페이지로)
- [ ] **9.2** 로그아웃 기능
- [ ] **9.3** (선택) 반응형·접근성 점검

---

## 진행 시 참고

- API 명세·curl 예시: [07-admin-api.md](07-admin-api.md)
- ERD·API 개요: [05-erd-api-spec.md](05-erd-api-spec.md) § 2.9 관리자
