<?xml version="1.0" encoding="utf-8"?>
<!-- Profile builds: same as debug — allow HTTP to dev server / LAN IP. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
